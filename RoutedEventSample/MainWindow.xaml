<Window x:Class="RoutedEventSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RoutedEventSample"
        mc:Ignorable="d"
        Title="Routed Event Sample Application" Height="450" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Routed Event Sample - Quantity Controls" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- StackPanel that will receive routed events from child controls -->
        <StackPanel Grid.Row="1" x:Name="MainPanel" 
                    Margin="0,0,0,20">
            <TextBlock Text="Multiple QuantityBox controls:" FontWeight="SemiBold" Margin="0,0,0,10"/>
            
            <UniformGrid Columns="3" Margin="0,0,0,10">
                <local:QuantityBox x:Name="qty1" Margin="5"/>
                <local:QuantityBox x:Name="qty2" Margin="5"/>
                <local:QuantityBox x:Name="qty3" Margin="5"/>
            </UniformGrid>
            
            <TextBlock Text="More QuantityBox controls:" FontWeight="SemiBold" Margin="0,0,0,10"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <local:QuantityBox x:Name="qty4" Margin="5"/>
                <local:QuantityBox x:Name="qty5" Margin="5"/>
            </StackPanel>
        </StackPanel>
        
        <GroupBox Grid.Row="2" Header="Event Log" Padding="10">
            <ScrollViewer>
                <TextBlock x:Name="eventLog" TextWrapping="Wrap" FontFamily="Consolas"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Window>