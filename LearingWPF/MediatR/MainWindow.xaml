<Window x:Class="WpfMediatRLearningApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MediatR WPF Learning App" Height="450" Width="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input Area -->
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBox Width="300" Height="30" 
                     Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalContentAlignment="Center" Padding="5"/>
            
            <Button Content="Add Customer" Width="100" Margin="10,0,0,0"
                    Command="{Binding AddCustomerCommand}" />
            
            <Button Content="Refresh List" Width="100" Margin="10,0,0,0"
                    Command="{Binding LoadCustomersCommand}" />
        </StackPanel>

        <!-- Loading Indicator -->
        <TextBlock Grid.Row="1" Margin="0,10,0,10" Text="Processing..." Foreground="Gray"
                   Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />

        <!-- Data List -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Customers}" DisplayMemberPath="Name" 
                 BorderBrush="LightGray" Padding="5"/>
    </Grid>
</Window>
