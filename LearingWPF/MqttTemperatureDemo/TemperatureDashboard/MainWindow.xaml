<Window x:Class="TemperatureDashboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MQTT Temperature Dashboard"
        Height="450"
        Width="700"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Factory Temperature Dashboard"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button x:Name="ConnectButton"
                    Content="Connect"
                    Width="120"
                    Height="35"
                    Margin="0,0,10,0"
                    Click="ConnectButton_Click"/>

            <Button x:Name="DisconnectButton"
                    Content="Disconnect"
                    Width="120"
                    Height="35"
                    Click="DisconnectButton_Click"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1"
                CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock x:Name="ConnectionStatusText"
                           Text="Broker Status: Disconnected"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>

                <TextBlock x:Name="MachineStatusText"
                           Text="Machine Status: Unknown"
                           FontSize="16"
                           Margin="0,0,0,10"/>

                <TextBlock x:Name="TemperatureText"
                           Text="Temperature: --"
                           FontSize="40"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <TextBlock x:Name="AlertText"
                           Text="Alert: None"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="Green"/>
            </StackPanel>
        </Border>

        <ListBox x:Name="LogListBox" Grid.Row="3"/>
    </Grid>
</Window>