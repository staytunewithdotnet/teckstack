<Window x:Class="WpfLearningApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLearningApp"
        mc:Ignorable="d"
        Title="WPF Learning App" Height="700" Width="1000" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Navigation Menu -->
        <Border Grid.Column="0" Background="#2C3E50" Padding="10">
            <DockPanel>
                <TextBlock Text="WPF Learning App" FontSize="18" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20" 
                           DockPanel.Dock="Top"/>
                
                <ScrollViewer DockPanel.Dock="Top">
                    <StackPanel x:Name="MenuPanel" Orientation="Vertical" />
                </ScrollViewer>
            </DockPanel>
        </Border>
        
        <!-- Main Content Area -->
        <Border Grid.Column="1" Background="White" Padding="20">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" />
        </Border>
    </Grid>
</Window>